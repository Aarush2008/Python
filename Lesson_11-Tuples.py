# Type of data structure which stores different values or information
coordinates = (4, 5)
# Tuple are usally shown as coordinates
# Tuple is inmutable, it cant be changed or modified.
print(coordinates[0])
# 4 is in index position 0 while  is in 1
# In lists you can substitue the value as something else and you use square brackets while in tuples you cant change anything and you use normal brackets
coordinates = (4, 5), (6, 7), (60, 34)
